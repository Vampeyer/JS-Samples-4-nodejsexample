

// Blurring the entire page  . 

/*
const elements = document.querySelectorAll('*');
elements.forEach(element => {
  element.style.filter = 'blur(5px)';
});
*/ 

  // Blurring each element 
  /*
  const elements = document.querySelectorAll('*');
elements.forEach(element => {
  element.style.filter = 'blur(5px)';
});   */